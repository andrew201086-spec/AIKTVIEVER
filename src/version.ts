/**
 * Identity of the build, printed on every report.
 *
 * A document that leaves the clinic has to say which program and which version
 * produced it — it is the first thing asked for in any review of medical
 * software, and the only way to tell whether a finding came from a build that
 * has since been corrected.
 */
export const APP_NAME = 'Просмотр КЛКТ';
export const APP_VERSION = 'v0.3.0';
